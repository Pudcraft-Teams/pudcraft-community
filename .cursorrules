# ============================================================
# Pudcraft Community — 项目规范、约束与注意事项
# ============================================================
# 本文件是 AI 协作编码的核心约束文件。
# 所有涉及代码修改、架构决策、新增功能的操作都必须遵守以下规则。
# ============================================================

## 1. 文件 / 目录组织规则

### 目录职责（严格分离）

| 目录                       | 职责                                 | 禁止放入                         |
| -------------------------- | ------------------------------------ | -------------------------------- |
| `src/app/`                 | Next.js App Router 页面 & API 路由   | 业务逻辑、工具函数               |
| `src/app/api/`             | REST API Route Handlers              | 页面组件、抓取逻辑               |
| `src/components/`          | 可复用 UI 组件                       | API 调用、数据库访问             |
| `src/lib/`                 | 工具函数、第三方服务封装、DB 客户端  | React 组件、路由定义             |
| `src/lib/serverStatus/`    | Minecraft 服务器状态相关逻辑         | 队列定义、UI 代码                |
| `src/queues/`              | BullMQ 队列定义与 enqueue 方法       | Worker 处理逻辑                  |
| `src/worker/`              | 后台 Worker 进程入口                 | API Route、页面组件              |
| `src/styles/`              | 全局样式                             | 组件级样式（用 Tailwind 类名）   |
| `prisma/`                  | Prisma Schema 和数据库迁移           | 应用代码                         |

### 命名规范

- **组件文件**：PascalCase（如 `ServerCard.tsx`）
- **工具/库文件**：camelCase（如 `validation.ts`、`statusQueue.ts`）
- **页面文件**：`page.tsx`（Next.js 约定）
- **API Route**：`route.ts`（Next.js 约定）
- **组件名**：PascalCase（`export function ServerCard`）
- **函数/变量**：camelCase（`export function enqueueStatusCheck`）
- **常量**：UPPER_SNAKE_CASE（`const QUEUE_NAME = "server-status"`）
- **类型/接口**：PascalCase，不加 `I` 前缀（`interface ServerCardProps`）
- **Zod Schema**：camelCase + `Schema` 后缀（`createServerSchema`）
- **数据库表名**：snake_case 复数，通过 `@@map` 映射（`@@map("servers")`）
- **数据库字段映射**：snake_case，通过 `@map` 映射

### 新增文件规则

- 新的 UI 组件 → `src/components/`
- 新的页面 → `src/app/` 下对应路由
- 新的 API → `src/app/api/` 下对应路径
- 新的工具函数 → `src/lib/`
- 新的队列 → `src/queues/`
- 新的 Zod Schema → `src/lib/validation.ts`（集中管理）

---

## 2. 提交规则

- 每次提交**聚焦于单一变更**：一个功能 / 一个修复 / 一次重构
- **不要**在功能提交中混入重构代码
- **不要**在重构提交中混入功能变更
- **不要**在一次提交中同时修改前端和后端（除非它们紧密耦合）
- Commit message 推荐格式：`<type>: <description>`
  - `feat:` 新功能
  - `fix:` 修复 Bug
  - `refactor:` 重构（无功能变化）
  - `style:` 代码格式调整（非 UI 样式）
  - `docs:` 文档更新
  - `chore:` 构建/配置/依赖变更
  - `perf:` 性能优化
- 提交前运行 `pnpm lint` 确保无报错
- 提交前确认 `.env` 未被 stage

---

## 3. 安全规则

### 密钥管理

- **绝不**把任何密钥、Token、密码、连接字符串写入代码或仓库
- 所有敏感配置通过 `.env` 文件管理
- 仓库中只保留 `.env.example`（只有键名，无真实值）
- `.env` / `.env.local` / `.env.production` 已加入 `.gitignore`
- 每次提交前 **务必确认** 没有意外包含 `.env`

### API 输入校验

- 所有 API 端点的输入 **必须** 使用 Zod Schema 校验
- **不要信任** 任何来自客户端/外部的数据
- `host` 字段：正则校验格式，禁止 IP 直连（防 SSRF），禁止 `localhost` / `127.0.0.1` / 内网 IP
- `port` 字段：限制 1-65535 范围
- 字符串字段：限制长度上限
- 数组字段：限制元素数量上限

### 速率限制（预留点）

- 所有公开 API Route 预留中间件挂载点
- 建议未来在 `src/lib/rateLimit.ts` 中实现
- 推荐方案：`@upstash/ratelimit` 或基于 Redis 的滑动窗口

### 外链处理（预留点）

- 用户提交的外部链接必须添加 `rel="noopener noreferrer"` 和 `target="_blank"`
- Markdown 渲染时必须过滤危险 HTML 标签（XSS 防护）
- 图片 URL 建议做域名白名单校验
- 未来在 `next.config.ts` 的 `images.remotePatterns` 中配置

---

## 4. 性能规则

### 核心原则

- **禁止**在页面请求（Server Component / API Route）中直接 ping Minecraft 服务器
- 服务器状态 **必须** 通过 Worker 异步获取，结果写入数据库
- 前端从 DB/缓存读取状态数据，允许有 1-5 分钟延迟
- 页面渲染不得依赖任何外部网络请求的实时结果

### 超时策略

| 场景                    | 超时时间  |
| ----------------------- | --------- |
| MC 服务器 ping          | 5 秒      |
| Worker 单任务处理       | 10 秒     |
| API Route 响应目标      | < 200ms   |
| 数据库查询告警阈值      | > 100ms   |

### 重试策略

- Worker 任务失败后使用 **指数退避** 重试
- 最大重试次数：3
- 退避序列：2s → 4s → 8s
- 超过最大重试后任务标记失败，保留日志

### 缓存策略（建议）

- 服务器列表：使用 Next.js ISR（`revalidate: 60`）或短 TTL 缓存
- 单服务器状态：Redis 缓存 TTL 60-120 秒
- 静态资源：利用 Next.js 自动优化

---

## 5. UI 规则

### 设计原则

- **默认深色主题**：`bg-gray-950` 为主背景，`text-gray-100` 为默认文字
- **信息密度适中**：卡片内边距 `p-5`，元素间距 `gap-3` 到 `gap-4`
- **可读性优先**：文字对比度要充足，正文最小 `text-sm`，辅助信息 `text-xs`
- **克制原则**：不使用渐变背景、不使用 3D 阴影、不使用复杂动画
- **颜色一致性**：使用 Tailwind 内置 `gray` + `emerald` 色板

### ServerCard 卡片字段顺序（固定）

1. 服务器名称 + 在线状态指示灯（右上角）
2. 服务器地址（`host:port`，非默认端口才显示端口）
3. 简短描述（最多 2 行截断 `line-clamp-2`）
4. 在线人数 / 最大人数 + 延迟毫秒
5. 标签 Chips

### 颜色语义（固定）

| 语义             | 颜色            |
| ---------------- | --------------- |
| 在线             | `emerald-400`   |
| 离线             | `gray-600`      |
| 低延迟 < 50ms   | `emerald-400`   |
| 中延迟 50-100ms  | `yellow-400`    |
| 高延迟 > 100ms   | `red-400`       |
| 标签 Chip 背景   | `gray-800`      |
| 标签 Chip 文字   | `gray-400`      |
| 卡片边框         | `gray-800`      |
| 卡片 hover 边框  | `gray-600`      |

### 组件规范

- 所有 UI 组件放在 `src/components/`
- 使用 **命名导出**（`export function`），不使用默认导出
- 组件文件名与组件名一致（PascalCase）
- Props 使用独立的 `interface` 定义
- 组件需有简短的 JSDoc 功能说明

---

## 6. 数据库规则

### Prisma 模型变更流程

1. 修改 `prisma/schema.prisma`
2. 开发环境：`pnpm db:migrate --name describe_your_change`
3. 迁移命名使用描述性 snake_case：`add_server_claim_token`、`create_channel_tables`
4. 生产环境：`prisma migrate deploy`
5. **禁止**在生产环境使用 `db:push`

### 时间字段统一

- 所有时间字段使用 `DateTime` 类型
- 创建时间：`createdAt DateTime @default(now()) @map("created_at")`
- 更新时间：`updatedAt DateTime @updatedAt @map("updated_at")`
- 检查时间：`checkedAt DateTime @default(now()) @map("checked_at")`

### 模型设计规范

- 表名：snake_case 复数，通过 `@@map("table_name")` 映射
- 字段名：TypeScript 侧 camelCase，数据库侧通过 `@map("snake_case")` 映射
- ID：使用 `cuid()` 生成（`@id @default(cuid())`）
- 关联：必须显式定义 `onDelete` 行为（`Cascade` / `SetNull` / `Restrict`）
- 索引：查询频繁的字段必须建立索引
- **禁止**将大文件（图片/视频）直接存入数据库，使用对象存储

---

## 7. Worker 规则

### 任务幂等性

- 队列任务 **必须是幂等的**（重复执行不产生副作用）
- 使用 `jobId` 做去重：`jobId: \`status-\${serverId}\``
- 同一个 serverId 不会同时存在两个未完成的检查任务

### 失败处理

- 使用 BullMQ 的 `backoff.type: "exponential"` 指数退避
- 最大重试次数：3（`attempts: 3`）
- 失败任务保留最近 5000 条（`removeOnFail: { count: 5000 }`）
- 成功任务保留最近 1000 条（`removeOnComplete: { count: 1000 }`）
- 所有失败必须记录日志，包含 jobId、serverId、错误信息、重试次数

### 日志格式

- 使用 `src/lib/logger.ts` 统一日志输出
- 格式：`[ISO时间] [LEVEL] 消息 {JSON元数据}`
- Worker 日志 **必须包含**：jobId、serverId
- **禁止**在 Worker 中使用裸 `console.log`

### 并发控制

- Worker 并发数：5（`concurrency: 5`）
- 限流：每秒最多处理 10 个任务（`limiter: { max: 10, duration: 1000 }`）
- 可根据服务器资源和 MC 服务器数量调整

### Worker 进程管理

- Worker 作为独立进程运行（`pnpm worker:dev`）
- 必须实现优雅退出（监听 `SIGTERM` / `SIGINT`）
- 退出时等待当前任务完成再关闭连接

---

## 8. 代码风格

### TypeScript 严格模式

- `tsconfig.json` 中 `strict: true` **不可关闭**
- **禁止**使用 `any` 类型（ESLint 已配置 `@typescript-eslint/no-explicit-any: "error"`）
- 需要宽泛类型时使用 `unknown` + 类型守卫
- 类型导出使用 `export type`
- 类型导入使用 `import type`

### 函数 / 组件注释

- 所有导出函数 **必须** 有 JSDoc 注释
- 说明：用途、参数含义、返回值、副作用
- 组件需有简短的功能说明
- 复杂逻辑处写行内注释解释 **"为什么"**，而非 "是什么"
- **不要**写只重复代码内容的废话注释

### 导入顺序（从上到下）

1. Node.js 内置模块（`import { readFile } from "fs/promises"`）
2. 第三方库（`import { z } from "zod"`）
3. `@/` 路径别名导入（`import { prisma } from "@/lib/db"`）
4. 相对路径导入（`import { helper } from "./utils"`）
5. 类型导入放在同组最后（`import type { ... } from "..."`）

### 错误处理

- API Route **必须** 有 `try-catch` 包裹
- 错误响应统一格式：`{ error: string, details?: unknown }`
- 使用 `logger.error()` 记录错误，**不要吞掉异常**
- Worker 中的错误通过 BullMQ 自动重试，但仍需记录日志

### 导出规范

- 组件：命名导出（`export function ServerCard`）
- 工具函数：命名导出
- 类型：`export type` / `export interface`
- 常量：命名导出
- **禁止** default export（Next.js 页面组件除外，因为 App Router 要求）

---

## 9. 未来扩展点（约束与接口建议）

### 9.1 Markdown 渲染（服务器详情页）

- 实现位置：`src/components/MarkdownRenderer.tsx`
- 渲染库：`react-markdown` + `remark-gfm`（GFM 语法支持）
- **必须做 XSS 过滤**：使用 `rehype-sanitize`
- 图片 URL 需做域名白名单
- Server 模型预留 `content: String? @db.Text` 字段
- 详情页 `src/app/servers/[id]/page.tsx` 已预留渲染占位区域

### 9.2 服务器认领（MOTD Token 验证）

- Server 模型预留 `claimToken: String? @unique`
- 认领流程设计：
  1. `POST /api/servers/:id/claim` → 生成唯一 token，返回给用户
  2. 用户将 token 添加到 MC 服务器 MOTD
  3. Worker 定时 ping 目标服务器，检查 MOTD 中是否包含 token
  4. 验证通过 → 更新 `Server.ownerId`，清除 `claimToken`
- Token 生成：使用 `crypto.randomUUID()` 或 cuid
- Token 有效期：建议 24 小时
- 安全约束：同一用户同时只能有一个待验证的认领请求

### 9.3 频道系统（每服文本消息）

- 数据模型建议（见 schema.prisma 注释）：
  - `Channel { id, serverId, name, type }`
  - `Message { id, channelId, userId, content, createdAt }`
- 实时消息：WebSocket 或 Server-Sent Events (SSE)
- 路由预留：`/api/servers/:id/channels/`
- 约束：
  - 消息内容长度限制：最大 2000 字符
  - 消息频率限制：每用户每分钟最多 30 条
  - 敏感词过滤：预留 `src/lib/contentFilter.ts`
  - 只有服务器 owner 和 admin 可创建/删除频道

---

## 10. 禁止事项（红线）

1. ❌ **不要**把 Mock 数据和真实 DB 查询逻辑混在同一个函数里
   - Mock 数据隔离在 `src/lib/mock.ts`
   - 切换为真实数据时，替换数据源调用即可，不修改业务逻辑
2. ❌ **不要**写未经 Zod 校验的直接入库操作
3. ❌ **不要**在 API Route 中调用 `pingMinecraftServer`
   - 抓取逻辑 **只能** 在 Worker 中执行
4. ❌ **不要**在组件中直接 `fetch` 非本项目的外部 API
5. ❌ **不要**使用 `any` 类型
6. ❌ **不要**在提交中包含 `.env` 文件
7. ❌ **不要**在生产环境使用 `db:push`（只能用 `migrate deploy`）
8. ❌ **不要**把大文件（图片/视频/二进制）直接存入数据库
9. ❌ **不要**在 Server Component 中使用 `useState` / `useEffect`
10. ❌ **不要**随意修改 `docker-compose.yml` 端口映射（修改后必须同步更新 `.env`）
11. ❌ **不要**在页面请求路径中执行任何可能超时的外部网络调用
12. ❌ **不要**引入未经评估的重型依赖（先评估包大小和必要性）
13. ❌ **不要**在一次 PR/提交中同时进行功能开发和代码重构
14. ❌ **不要**硬编码配置值（端口、URL、超时等），统一使用环境变量或常量
